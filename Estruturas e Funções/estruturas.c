#include <stdio.h>

  // Função genérica para entrada de dados
  void entradaDados(){
  // Código para a função entradaDados
  }

  // Função genérica para cálculo da média
  float calcularMedia(){
  // Código para a função calcularMedia
  }

  // Função genérica para exibir dados
  void exibirMedia(){
  // Código para a função exibirMedia
  }

int main(){

  entradaDados();
  float media = calcularMedia();
  exibirMedia();

/*
  if (Condição){
      Bloco de código a ser executado
    }
  else {
      Bloco de código a ser executado
  }
*/

/*
  Operadores Aritméticos
  -Soma(+)
  -Subtração(-)
  -Multiplicação(*)
  -Divisão(/)
*/


/*
  int numero1, numero2;
  int soma, subtracao, multiplicacao, divisao;
  
  printf("Entre com o primeiro número:\n");
  scanf("%d", &numero1);
  printf("Entre com o segundo número:\n");
  scanf("%d", &numero2);

  soma = numero1 + numero2;
  subtracao = numero1 - numero2;
  multiplicacao = numero1 * numero2;
  divisao = numero1 / numero2;
  
  printf("A Soma do numero1 + numero2 é: %d\n", soma);
  printf("A Subtração do numero1 - numero2 é: %d\n", subtracao);
  printf("A Multiplicação do numero1 * numero2 é: %d\n", multiplicacao);
  printf("A Divisão do numero1 / numero2 é: %d\n", divisao);
*/


/*
  Operadores de Atribuição
    -Atribuição Simples(=)
    -Atribuição com Soma(+=)
    -Atribuição com Subtração(-=)
    -Atribuição com Multiplicação(*=)
    -Atribuição com Divisão(/=)
*/

/*
  int numero1 = 10, numero2 = 20, resultado;

  resultado = 10;
  printf("Resultado: %d\n", resultado);

  resultado += 20;
  printf("Resultado: %d\n", resultado);

  resultado -= numero1;
  printf("Resultado: %d\n", resultado);
  
  resultado *= 5;
  printf("Resultado: %d\n", resultado);

  resultado /= 2;
  printf("Resultado: %d\n", resultado);
*/

/*
   Operadores de incremento e decremento
   incremento (++)
   decremento (--)
*/

return 0;
}
